name: edupulse_ai
description: EduPulse AI — Smart Study App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.2.1
  supabase_flutter: ^2.5.6
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  go_router: ^13.2.1
  shimmer: ^3.0.0
  file_picker: ^8.0.3
  flutter_markdown: ^0.7.3+1
  intl: ^0.20.3
  fl_chart: ^0.68.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
